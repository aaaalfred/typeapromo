/**
 * Tema visual del formulario.
 *
 * Convierte el `ThemeDefinition` del contrato en variables CSS. Es la única
 * fuente de verdad del aspecto: el renderer no aplica colores con clases
 * condicionales, de modo que la previsualización del editor y la experiencia
 * pública no pueden verse distintas.
 */

export * from './fuentes';
export * from './tokens';
